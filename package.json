{
  "name": "mercadinho-do-bairro",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jogo de mercadinho em português, para computador e celular.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@fontsource/dm-sans": "5.3.0",
    "@fontsource/outfit": "5.3.0",
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.1.7"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
